import * as React from "react";
import Layout from "../components/common/Layout";

const AboutPage = ({ location }: any) => {
  return (
    <Layout path={location.pathname}>
      <main>준비중입니다.</main>
    </Layout>
  );
};

export default AboutPage;
